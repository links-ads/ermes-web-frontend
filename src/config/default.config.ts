export const DefaultConfig = {
  ui: {
    availableThemes: ['dark', 'light'],
    defaultTheme: 'light'
  }
  // add meta: name, description, envBanner?: string (es. dev/test...)
  // add supportedLanguages: ['en']
  // add map themes/styles
}
